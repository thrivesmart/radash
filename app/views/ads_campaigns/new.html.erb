<% content_for :page_title do %>
Add an Ad to Your Campaign - 
<% end %>
<div class="container">
  <div class="page-header">
    <h1>
      Run Ad
      <small><%= @org.name %></small>
    </h1>
  </div>

  <%= render 'form' %>

  <div class="clearfix"></div>
  <%= link_to "<span class=\"glyphicon glyphicon-ban-circle\" aria-hidden=\"true\"></span> Cancel".html_safe, org_campaigns_path(@org), class: "btn btn-default" %>
</div>
